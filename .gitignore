# Node dependencies — GitHub Actions runs "npm install" itself during
# the build, so this never needs to be committed.
node_modules/

# Eleventy build output
_site/

# TinaCMS build output and codegen (regenerated by `npm run build`)
admin/
tina/__generated__/
