# Node dependencies — Netlify runs "npm install" itself during the
# build, so this never needs to be committed.
node_modules/

# Local Netlify folder
.netlify

# Eleventy build output
_site/

# TinaCMS build output and codegen (regenerated by `npm run build`)
admin/
tina/__generated__/
